# LevelDB.cr

## Installation

1. Add the dependency to your `shard.yml`:

   ```yaml
   dependencies:
     leveldb:
       github: kojix2/leveldb
   ```

2. Run `shards install`

## Usage

```crystal
require "leveldb"
```
